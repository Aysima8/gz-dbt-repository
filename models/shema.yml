version: 2

sources:
  - name: raw
    schema: gz_raw_data   # BigQuery dataset adı

    tables:
      - name: sales
        identifier: raw_sales

      - name: product
        identifier: raw_products

      - name: ship
        identifier: raw_ship